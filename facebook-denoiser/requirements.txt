# Core model + helpers
denoiser==0.1.5     # Official PyPI release of facebookresearch/denoiser
torch               # Install CPU by default; replace with CUDA-specific wheel if needed
torchaudio          # Must match your torch version
soundfile
tqdm

# Note: You need the ffmpeg executable on your PATH for format conversion.
#   macOS:    brew install ffmpeg
#   Ubuntu:   sudo apt-get update && sudo apt-get install -y ffmpeg
#   Windows:  choco install ffmpeg  (or download from ffmpeg.org)
